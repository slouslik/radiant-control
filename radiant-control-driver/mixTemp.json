{
  "name": "Mix Temperature",
  "attributes": {
    "mixTemp": {
      "schema": {
        "type": "object",
        "properties": {
          "value": { "type": "integer", "minimum": 0, "maximum": 150 },
          "unit": { "type": "string", "enum": ["F"], "default": "F" }
        },
        "additionalProperties": false,
        "required": ["value"]
      }
    }
  }
}
