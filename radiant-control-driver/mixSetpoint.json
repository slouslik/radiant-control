{
  "name": "Mixing Valve Setpoint",
  "attributes": {
    "mixSetpoint": {
      "schema": {
        "type": "object",
        "properties": {
            "value": { "type": "integer", "minimum": 40, "maximum": 150 },
            "unit": { "type": "string", "enum": ["F"], "default": "F" }
        },
        "additionalProperties": false,
        "required": ["value"]
      },
      "setter": "setMixSetpoint"
    }
  },
  "commands": {
    "setMixSetpoint": {
      "arguments": [
        {
          "name": "value",
          "optional": false,
          "schema": { "type": "integer", "minimum": 40, "maximum": 150 }
        }
      ]
    }
  }
}

